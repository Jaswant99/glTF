{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "Image features",
    "type": "object",
    "description": "Extension to add KTX file format, cube and mip maps to the image node.",
    "allOf": [ { "$ref": "glTFChildOfRootProperty.schema.json" } ],
    "properties": {
        "mimeType": {
            "anyOf": [
                {
                    "enum": [ "image/ktx" ]
                },
                {
                    "type": "string"
                }
            ],
            "description": "The image's MIME type. Required if `bufferView` is defined."
        },
        "mipLevels": {
            "type": "integer",
            "description": "The number of mip levels of this image.",
            "minimum": 1,
            "default": 1
        },
        "faces": {
            "type": "integer",
            "description": "The number of faces of this image.",
            "minimum": 1,
            "default": 1
        }
    }
}